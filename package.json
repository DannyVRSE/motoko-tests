{
  "scripts": {
    "test": "vitest",
    "ci": "vitest run",
    "preci": "dfx stop; dfx start --clean --background; dfx deploy; dfx generate"
  },
  "devDependencies": {
    "isomorphic-fetch": "^3.0.0",
    "vitest": "^2.1.1"
  },
  "dependencies": {
    "@dfinity/agent": "^2.1.1"
  }
}